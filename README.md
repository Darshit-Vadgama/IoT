Use No OTA partition in Esp32 due to high data size - Huge App (3MB NO OTA)
On boot up, ESP32 will give a device ID, which needs to be passed as an argument while running the Python script for QR generation
Scan the QR, connect the device to Bluetooth, and share wifi credentials
